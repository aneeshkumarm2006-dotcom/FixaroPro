export const metadata = {
  title: "Book a cleaning — Cleano",
  description: "Book a professional cleaning service in just a few clicks.",
};

export default function BookLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
